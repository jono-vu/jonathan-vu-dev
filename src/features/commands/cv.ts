import { constants } from "../../config";

const cv = {
  input: "cv",
  output: [
    null,
    `Find my Resume/CV here:`,
    `↳ https://jonathan-vu.dev/cv`,
    null,
    constants.COMMAND_PREFIX,
  ],
};

export { cv };
